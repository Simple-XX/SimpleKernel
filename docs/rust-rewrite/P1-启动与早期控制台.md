# 阶段 1：启动与早期控制台

> **依赖**：阶段 0 | **预计时间**：2 周 | **复杂度**：中

## 目标
内核启动，解析 FDT，并在两个架构的 UART 上打印 "Hello SimpleKernel"。

## 需要创建的文件
- `kernel/src/arch/riscv64/init.rs` — `ArchInit()` (FDT 解析，早期控制台)
- `kernel/src/arch/riscv64/console.rs` — SBI putchar
- `kernel/src/arch/aarch64/init.rs` — `ArchInit()` (FDT 解析，早期控制台)
- `kernel/src/arch/aarch64/console.rs` — PL011 MMIO putchar
- `kernel/src/logging.rs` — `klog` 替代方案：基于 MPMC 队列（256 条目 × 256 字节 = 64KB 静态），使用原子 try-lock 的非阻塞排空，每个条目包含 {seq, core_id, level, msg[239]}，编译时级别过滤，丢弃计数，用于 panic 路径的 `RawPut`
- `kernel/src/fdt.rs` — 使用 `fdt` crate 的 FDT 包装器
- `kernel/src/error.rs` — `ErrorCode`, `KernelError`, `KResult<T>`
- `kernel/src/config.rs` — 常量（最大核心数等）
- `kernel/src/per_cpu.rs` — Per-CPU 数据结构
- `kernel/src/mmio.rs` — MMIO 寄存器访问助手

## 需要复用的文件
无

## C++ 参考文件
- `src/arch/riscv64/arch_main.cpp` → `kernel/src/arch/riscv64/init.rs`
- `src/arch/riscv64/early_console.cpp` → `kernel/src/arch/riscv64/console.rs`
- `src/arch/aarch64/arch_main.cpp` → `kernel/src/arch/aarch64/init.rs`
- `src/arch/aarch64/early_console.cpp` → `kernel/src/arch/aarch64/console.rs`
- `src/include/expected.hpp` → `kernel/src/error.rs`
- `src/include/kernel_fdt.hpp` → `kernel/src/fdt.rs`
- `src/include/kernel_log.hpp` → `kernel/src/logging.rs`
- `src/include/per_cpu.hpp` → `kernel/src/per_cpu.rs`
- `src/include/kernel_config.hpp` → `kernel/src/config.rs`
- `src/include/mmio_accessor.hpp` → `kernel/src/mmio.rs`
- `src/include/basic_info.hpp` → 集成到 `per_cpu.rs`

## 实现步骤
- [ ] 步骤 1：实现 `error.rs` — `ErrorCode` 枚举, `KernelError`, `KResult<T>`
- [ ] 步骤 2：实现 `config.rs` — 从 `kernel_config.hpp` 移植常量
- [ ] 步骤 3：实现 `mmio.rs` — volatile MMIO 读/写助手
- [ ] 步骤 4：实现 `logging.rs` — 带有 UART 输出的 `log` crate 后端
- [ ] 步骤 5：为 riscv64 实现早期控制台（SBI ecall 用于 putchar）
- [ ] 步骤 6：为 aarch64 实现早期控制台（PL011 MMIO）
- [ ] 步骤 7：实现 `fdt.rs` — 对 `fdt` crate 的薄包装
- [ ] 步骤 8：实现 `per_cpu.rs` — PerCpu 结构体、数组和访问器
- [ ] 步骤 9：实现 `arch/riscv64/init.rs` — ArchInit（解析 FDT，设置控制台，SMP 数据）
- [ ] 步骤 10：实现 `arch/aarch64/init.rs` — ArchInit（解析 FDT，设置控制台，SMP 数据）
- [ ] 步骤 11：将 `main.rs` 连接到 ArchInit 并打印 "Hello SimpleKernel"
- [ ] 步骤 12：在 riscv64 QEMU 上测试 —— 验证控制台输出
- [ ] 步骤 13：在 aarch64 QEMU 上测试 —— 验证控制台输出
- [ ] 步骤 14：提交

## QEMU 预期输出
```
U-Boot 20XX.XX (...)
...
## Loading kernel from FIT Image ...
[0][0 0 INFO ] FDT: found 25 nodes, 4 CPUs
[1][0 0 INFO ] Memory: 1024 MB
[2][0 0 INFO ] Hello SimpleKernel
```

## 验证命令
```bash
cargo build --target targets/riscv64gc-simplekernel.json
cargo xtask run --arch riscv64
# 观察串口输出，确认 "Hello SimpleKernel" 出现
# Ctrl+A, X 退出
```

## 退出标准
1. "Hello SimpleKernel" 出现在两个架构的 QEMU 串口输出中
2. 日志格式为 `[seq][core_id printer_core LEVEL]`
3. FDT 解析成功（显示节点数和 CPU 数）
4. Per-CPU 数据初始化完成

## 你可以修改的内容
- `logging.rs` — 日志格式、级别过滤、队列大小
- `console.rs` — 串口驱动实现
- `fdt.rs` — FDT 解析逻辑、节点遍历方式
- `init.rs` — 启动流程、初始化顺序
- `error.rs` — 错误码定义、错误消息

---
> 返回 [总览](./00-概述.md) | 下一阶段：[P2 — 核心基础设施](./P2-核心基础设施.md)
